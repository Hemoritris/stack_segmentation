# 统一换行符
* text=auto

# 大模型/数据文件如需入库，建议改用 Git LFS（先执行 `git lfs install`）
# *.pt   filter=lfs diff=lfs merge=lfs -text
# *.onnx filter=lfs diff=lfs merge=lfs -text
# *.bag  filter=lfs diff=lfs merge=lfs -text
# *.pcd  filter=lfs diff=lfs merge=lfs -text

